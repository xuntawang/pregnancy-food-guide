# 个护功能发布核查清单

核查日期：2026-07-28

核查范围：统一饮食／个护单页、个护医学数据、商品快照、成分表分析器、移动端与无脚本回退。

发布结论：本地发布前核查通过；真实部署与真实移动设备项目保持待办。

## 自动化核查

| 核查项 | 结果 | 证据 |
| --- | --- | --- |
| 语法、数据、引擎、UI 与饮食回归 | 通过 | `npm run check`：56/56 通过，0 失败，0 跳过 |
| 外部链接协议与数量 | 通过 | `index.html` 精确统计：265 处，92 个唯一 URL，全部为 HTTPS；逐项归属与核查结论见 `docs/个护外部链接核查记录.md` |
| 医学数据契约 | 通过 | 82 条成分规则、53 条项目规则；强结论、途径、场景、条件与来源关联均由测试约束 |
| 商品快照契约 | 通过 | 30 个商品均含地区、版本、配方复核日期、完整配方与配方来源；结论运行时动态计算 |
| 日期有效性 | 通过 | 严格拒绝不存在的日历日期与未来复核日期；配方复核与医学复核独立显示 |
| 隐私与运行时依赖 | 通过 | 不读取剪贴板、不上传、不保存成分表；运行时无远程请求、外部脚本或样式依赖 |

## 医学内容复核

- [x] 对全部 `avoid`、`limit`、`consult` 规则做反向契约复核：页面结论不强于证据；途径与人群场景一致；配方来源不冒充医学依据。
- [x] 浓度、面积、频率、孕期阶段、破损皮肤、通风、职业暴露等条件均保留来源边界。
- [x] 未知成分和未知商品不判为“可以用”；未知商品明确引导粘贴当前包装完整成分表。
- [x] 商品卡分别展示配方来源与医学依据，并分别展示配方复核日期和医学依据复核日期。
- [x] 全部成分与项目的替代方案均通过统一反向契约：不会包含任一 `limit`、`avoid`、`consult` 成分／项目的名称或别名；交叉命中为 0。
- [x] BOTOX Cosmetic 使用 FDA 2024 完整处方标签；页面保持“选择性项目暂缓、医学必要用途共同权衡”的非恐慌表述。
- [x] 92 个唯一来源／配方链接已逐一核查并形成表格：84 个首轮 GET 成功；6 个 ACOG 页面首轮 402、默认 `curl -L` 实测 200；国家卫健委页面首轮 412、NIH ODS 页面首轮 403，二者均由实际 Chrome 打开目标标题与正文并留存截图。自动拦截状态仍保留在记录中。

## 移动端与浏览器核查

- [x] 当前 Chrome，375×812：饮食首页、个护首页、个护搜索、商品证据、成分扫描均完成截图复核。
- [x] 当前 Chrome，430×932：饮食首页、个护首页、商品证据、成分扫描均完成截图复核。
- [x] 两档视口均无页面级横向溢出；长 INCI 名和长证据链接可换行。
- [x] 可见的 tab、筛选、类别、清除、证据展开和扫描按钮均不小于 44×44 CSS px。
- [x] 键盘可用：左右方向键切换频道；Enter 操作建议筛选、类别筛选和证据详情；焦点轮廓为 3px。
- [x] 固定用例通过：视黄醇、水杨酸、染发、肉毒素、Differin 商品别名、未知商品。
- [x] 成分表用例通过：中英文混合分隔符、重复成分、空输入、限制成分加未知成分、超过 200 项截断。
- [x] 常规页面代表性操作后浏览器日志为 0 error、0 warning。
- [x] 无脚本等价渲染夹具确认静态饮食内容、医学免责声明和“需要启用 JavaScript”提示可见；脚本阻断时动态结果为空，不会伪装成可查询。
- [x] 375×500 收缩视口模拟软键盘占用：搜索框保持可见且有明确焦点，无可见固定底栏遮挡。

本轮精确视口证据位于 `.superpowers/sdd/2026-07-27-孕期个护速查实施计划/task-7-repair-1-audit/`：`01` 至 `04` 为 375×812，`05` 至 `08` 为 430×932；PNG 像素尺寸与 Chrome `innerWidth × innerHeight × DPR` 一致。`viewport-375x812-metrics.json` 与 `viewport-430x932-metrics.json` 记录了 DPR、页面宽度、横向溢出和控件边界框。`09`、`10` 分别是国家卫健委与 NIH ODS 的实际 Chrome 外链复核。

## 发布后核查

状态：**尚未执行**。本任务未获部署授权，以下项目不得提前标记完成。

- [ ] 在真实 HTTPS 部署地址复跑饮食与个护固定查询。
- [ ] 在 iOS Safari 与 Android Chrome 真机验证软键盘、滚动、触控和长链接换行。
- [ ] 在浏览器原生“禁用 JavaScript”设置下复核 noscript 提示。
- [ ] 复核部署响应头、缓存、404、控制台、外部链接与页面标题。
- [ ] 记录部署版本、时间、执行人、设备与截图；出现医学或交互回归时停止发布。
